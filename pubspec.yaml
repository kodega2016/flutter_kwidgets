name: reusefirebase
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^2.1.0
  cupertino_icons: ^1.0.2
  firebase_core: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
